# Copy to spec/helpers/acceptance/.env.acceptance to override the defaults.
# Every value is optional; shell variables take precedence over this file.

# Where the running service is listening, including the base path.
# ACCEPTANCE_BASE_URL=http://localhost:3000/api/recipes-service

# Milliseconds before a single HTTP call is aborted.
# ACCEPTANCE_REQUEST_TIMEOUT_MS=10000
