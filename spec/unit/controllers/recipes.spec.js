// const recipesController = require('../../../lib/controllers/recipes');
// const recipesService = require('../../../lib/services/recipes');

describe('Recipes Controller', () => {
  describe('#getRecipes', () => {
    it('should fetch the recipe names and ids');
    it('should pass errors to next');
  });

  describe('#getRecipe', () => {
    it('should fetch the recipe for the given id');
    it('should pass errors to next');
  });
});
